---
scope: Palier 9 — Refonte UI & lisibilite de l'etat (PRD 2026-08-01, FR-1..FR-44, D-1..D-10, Q-1..Q-14)
purpose: build-substrate
altitude: feature
updated: 2026-08-05T14:59
---

- (constraint) Herite P1-AD-1 : PrismaService global, aucun nouveau module ne le reimporte
- (constraint) Herite P1-AD-2 : mutations exclusivement en couche Service, jamais de Prisma dans un controller
- (constraint) Herite P1-AD-3 : PartiesService.getOwned/getViewable = seul point de verite d'appartenance/role par Partie
- (constraint) Herite P1-AD-4 : import type pour tout type @master-jdr/shared cote apps/api
- (constraint) Herite P1-AD-5 : Angular @if/@for, signals, jamais *ngIf/*ngFor
- (constraint) Herite P5-AD-4 : tout catalogue de choix fixes = ContentType/ContentEntry seede, jamais code en dur
- (constraint) Herite P6-AD-1 : arbitrage JSON-vs-relationnel — relationnel si stable/partage/interroge, JSON si specifique au personnage et charge en bloc
- (constraint) Herite P7-AD-2/AD-4 : contrat notifyChanged() + realtimeEvents.emit(topic) en fin de mutation, hors transaction
- (constraint) PRD P-5 : aucune evolution serveur silencieuse — 10 derogations actees D-1..D-10, toute nouvelle est remontee avant codage
- (constraint) PRD P-1 : jamais la couleur seule (icone, libelle OU typographie comme second signal)
- (constraint) PRD P-3 : desktop et mobile a parite, mobile desormais testable sur appareil reel
- (version) Aucun ajout de dependance envisage a ce stade — stack existante NestJS 11 / Prisma 7 / Angular 22 / Postgres 17 (a reverifier au finalize)
- (event) Brownfield sweep : User{id,email,pseudo,passwordHash,role,createdAt} sans champ de preference ; aucun modele Favorite/UserPreference ; Announcement sans etat de lecture ; Partie sans champ de cloture ; PollVote.answer contraint YES|NO|MAYBE, aucun DELETE de vote joueur
- (event) Brownfield sweep : toDto() (character.service.ts:1581) est le point de serialisation unique de CharacterDto — chokepoint naturel pour un filtrage serveur FR-23
- (event) Brownfield sweep : TONE_MAP = un seul objet Record<Theme,Record<string,string>> dans tones.ts (500 lignes, 3 themes) ; ThemeToneService persiste en localStorage (cle jdr-theme)
- (event) Brownfield sweep : ModeService (core/mode) porte la bascule MJ/joueur + mjParties/playerParties + notifyChanged() cable sur le topic SSE user:{id} — sa suppression (FR-7) doit preserver ce cablage
- (event) Brownfield sweep : GET /parties?role=mj|player (PartiesController.list) renvoie PartieDto sans aucun signal d'etat ; PartieDto = {id,name,kind,gameSystemId,description,mjId,createdAt,nextSessionDate,nextSessionSlot}
- (event) Brownfield sweep : GET /users/search = egalite stricte email OU pseudo, renvoie UserSearchResultDto{id,pseudo,email} (expose l'email)
- (direction) Mode coaching, livrable = spine seule
- (decision) Etat de compte : colonnes typees sur User (theme, hideFinishedParties) + 2 modeles relationnels PartieFavorite et AnnouncementRead. Binds D-1/D-3/FR-2/FR-3/FR-11/FR-13. Prevents : 4 stockages inventes par 4 stories (table cle/valeur ou blob JSON) et un filtre par statut non interrogeable en base. Choix utilisateur.
- (decision) Identite utilisateur dans les DTO : tout DTO exposant une identite porte pseudo ET displayName ; displayName NOT NULL, initialise au pseudo a la migration, aucun repli cote front. Exception : le DTO de recherche d'invitation ne porte QUE le pseudo (ni displayName ni email — corrige UserSearchResultDto qui expose l'email aujourd'hui, contraire a FR-30). Prevents : dix ecrans avec dix replis differents, et l'impossibilite de lever une homonymie (FR-4b) sans aller-retour serveur. Choix utilisateur (formule en usage : nom affiche dans les ecrans de jeu, pseudo dans la recherche).
- (event) Note pedagogique : 'DTO' explique a l'utilisateur = forme de l'objet JSON renvoye par l'API
- (decision) Signaux FR-12 : endpoint dedie GET /me/party-signals renvoyant une carte partieId -> signaux pour toutes les parties de l'utilisateur, calculee serveur en requetes groupees (jamais un appel par partie). Les listes par role gardent leur contrat. Binds FR-12/Q-11. Prevents : le fan-out par partie (2 bugs de prod deja vecus : rafales 429, listes videes) et un double calcul pour un utilisateur MJ+joueur. Choix utilisateur.
- (decision) ModeService devient MyPartiesService : retrait de mode/setMode, conservation a l'identique de mjParties/playerParties, du compteur anti-course seq et de notifyChanged() cable sur le canal SSE user:{id} ; ajout de la liste unifiee. Binds FR-7. Prevents : la reecriture du cablage SSE et des protections de course ecrites apres un bug de prod. Choix utilisateur.
- (decision) FR-23 filtrage : applique dans toDto() (character.service.ts), point de serialisation unique traverse par findOne/findByPartie ET par les 3 exports PDF (qui appellent findOne). Prevents : un filtre pose dans le controller de lecture, qui laisserait fuiter la fiche complete par le chemin PDF.
- (decision) FR-23 unite de verrouillage : declaree par le schema du systeme de jeu (getSchema), jamais codee en dur dans l'ecran de config. Chaque cle de sheetSchema est verrouillable en bloc ; une cle de type objet peut declarer ses sous-champs verrouillables individuellement (Ryuutama : narrative seul — sex/age/physicalTraits/homeTown/motivation/name/personality). Grain mixte demande par l'utilisateur : attributs et classe en bloc, narratif au champ. Prevents : un ecran de config a reecrire a chaque nouveau systeme (Paliers 11-12) et un champ ajoute plus tard qui echapperait au verrou.
- (decision) FR-23 stockage : colonne JSON Partie.sheetVisibility, ecrite d'un bloc depuis l'ecran de config, jamais interrogee par valeur. Choix utilisateur.
- (constraint) Requalification utilisateur : FR-23 est une preference de gameplay (anti-spoil), PAS un enjeu de securite. Defaut = rien de verrouille (les notes restent regies par leur mecanisme existant). Le filtrage reste serveur car un masquage a l'affichage se contourne par les devtools, donc ne protege meme pas du spoil.
- (question) Ecart existant releve : les 3 endpoints d'export PDF appellent findOne() — un joueur peut deja exporter la fiche complete d'un coequipier. Le filtrage en toDto() le corrige mecaniquement ; a signaler en revue.
- (decision) Q-2 tranchee : themes decoupes en un fichier par theme, un theme de reference (grimoire-emeraude) source du type, les deux autres types d'apres ses cles — cle manquante = erreur de compilation. Binds FR-43. Prevents : une decoupe qui perd la seule garantie que la structure actuelle offrait encore. Choix utilisateur.
- (decision) FR-14 : tout affichage d'un nom d'identite (joueur, personnage, ou les deux) passe par un composant d'affichage partage unique. L'AD fixe le MECANISME seulement ; le traitement visuel retenu (italique ou autre) est explicitement reporte a la passe d'UI. Choix utilisateur, avec sa reserve explicite : l'italique n'est qu'un exemple, surtout pas une regle a ce stade.
- (decision) FR-33 revise apres correction utilisateur : aucune seance d'une autre partie n'est jamais exposee dans le calendrier d'une partie. Une seance datee d'une autre partie d'un participant se traduit A LA LECTURE en une indisponibilite de ce participant, injectee dans l'agregation existante (AvailableSlotDto vue MJ / AggregatedSlotDto vue joueur) — jamais un nouveau canal de donnees, jamais une AvailabilityDeclaration persistee en double. Non-fuite structurelle : ce qui transite est un statut de creneau, jamais une identite de partie.
- (decision) Statut d'occupation fondu dans 'indisponible' — aucun nouveau SlotStatus. Choix utilisateur (contre ma recommandation d'un statut distinct : simplicite des couleurs et de l'agregation privilegiee).
- (decision) FR-34 : aucun changement serveur — les creneaux d'un sondage ouvert sont deja renvoyes par GET /parties/:id/poll, le calendrier les superpose.
- (decision) FR-44 : Partie.closedAt (DateTime nullable) — seule la decision du MJ est persistee, reversible en le vidant. 'En cours' et 'pas encore commencee' sont derives a la lecture. Binds FR-44/FR-3/FR-10/FR-12, D-9. Prevents : un champ status a trois valeurs dont personne ne pilote la transition 'a venir -> en cours' de facon fiable (une campagne en pause serait vue comme terminee). Meme forme que Scenario.closedAt deja en place. Choix utilisateur apres explication persiste-vs-derive.
- (decision) FR-35 / D-5 : le retrait d'un vote supprime la ligne PollVote (DELETE), il n'existe pas de reponse vide — l'absence de ligne signifie deja 'n'a pas repondu' (SlotStatus UNKNOWN dans l'agregation). Aucun elargissement de l'enum VoteAnswer.
- (decision) FR-16 / D-10 : GET /me/characters — liste des personnages de l'utilisateur toutes parties confondues, restreinte a ses propres personnages, meme famille de routes /me que les signaux (FR-12) et les preferences.
- (decision) Q-3 tranchee (choix utilisateur, plus exigeant que ma recommandation) : changement d'e-mail = mot de passe courant exige + avis a l'ancienne adresse + lien de confirmation sur la NOUVELLE adresse (le changement ne prend effet qu'au clic) + lien de retour arriere valide 1 mois envoye a l'ANCIENNE adresse, permettant de recuperer le compte en cas d'usurpation. Meme pattern de jeton que PasswordResetToken (tokenHash unique, usedAt, expiresAt).
- (decision) Q-4 tranchee : un changement de mot de passe en session coupe toutes les autres sessions et conserve la courante — reutilise l'index UserSession et le mecanisme deja implemente par AuthService.resetPassword(), pour que les deux chemins de changement de mot de passe se comportent identiquement. Choix utilisateur.
- (assumption) [ASSUMPTION] Le lien de retour arriere (Q-3) implique que l'attaquant connaissait le mot de passe : son activation restaure l'ancienne adresse, coupe TOUTES les sessions et force une reinitialisation de mot de passe. A confirmer par l'utilisateur.
- (decision) Gestion de compte : nouveau module apps/api/src/account/ portant les routes /me (profil, preferences, favoris, annonces vues), coherent avec 'un dossier par capacite' (P8-AD-6). La verification de mot de passe (argon2) et la coupure de sessions restent deleguees a AuthService, deja seul proprietaire de ces mecanismes — jamais reimplementees.
- (decision) FR-19/FR-20 : l'aide contextuelle lit les textes deja seedes au Palier 8 via GameSystemService.getContent() — aucun nouveau mecanisme de contenu, aucun texte de regle dans tones.ts (herite P8-AD-9). La FORME de consultation (Q-9) reste ouverte, tranchee a la conception de la fiche.
- (direction) Environnement/deploiement : aucun changement pour ce palier. Les 2 amenagements de dev (API_BASE calcule depuis window.location, WEB_ORIGIN multi-origines) restent explicitement propriete du Palier 10 — cette spine n'y touche pas.
- (decision) Retour arriere e-mail (Q-3) confirme : restaure l'ancienne adresse, coupe TOUTES les sessions, et force une reinitialisation de mot de passe avant toute reconnexion. Leve l'assumption precedente. Choix utilisateur.
- (event) Reviewer gate : lint deterministe OK (3 faux positifs 'partie:{id}'). 3 relecteurs en parallele (rubrique, versions/brownfield, adversarial) — rapports dans reviews/. Constats critiques verifies personnellement dans le code avant correction.
- (constraint) VERIFIE : RealtimeService ouvre un EventSource PAR topic connecte (urlForTopic = /parties/{id}/events ou /users/me/events). Un ecran de liste ne peut donc pas s'abonner a N topics partie: sans N connexions — AD-14 doit se cabler sur user: seul.
- (constraint) VERIFIE : AuthService.resetPassword() coupe TOUTES les sessions (deleteMany session + userSession), inline dans une transaction, non expose.
- (constraint) VERIFIE : DELETE /parties/:id/poll/:pollId appelle PollService.close() = status OPEN->CLOSED, ne supprime pas le sondage (Seance.pollId pointe dessus). PollVote est unique par [optionId, userId] : un retrait porte sur une option.
- (constraint) VERIFIE : toDto() est une fonction libre (character, ownerPseudo, ownerIsMj, viewerIsMj), synchrone, sans viewerId ni acces Prisma — le filtrage AD-7 impose un changement de signature propage a ~14 appels.
- (constraint) VERIFIE : PartiesService.listForUser/getViewable renvoient l'objet Prisma BRUT — ajouter sheetVisibility a Partie le ferait fuiter aux joueurs sans projection explicite.
- (constraint) VERIFIE : sheetSchema declare deja fields sur attributes (['AGI','ESP','INT','VIG']) — AD-7 ne peut pas reutiliser 'fields' pour designer les sous-champs verrouillables, sens inverse.
- (constraint) VERIFIE : PartieMemberDto expose email a tout coequipier ; Partie.nextSessionDate/nextSessionSlot sont deja persistes.
- (decision) Q-4 affine apres correction : la coupure de sessions est extraite en methode partagee revokeSessions(userId, exceptSid?) ; le changement en session conserve la session courante (exceptSid = req.sessionID), la reinitialisation par e-mail continue de tout couper. Divergence assumee et ecrite comme telle. Choix utilisateur conditionnel ('si c'est simple et sans grosse faille') — condition remplie : la coupure existe deja, exclure un sid est trivial, et le titulaire de la session courante vient de prouver qu'il connait le mot de passe.
- (decision) E-mails dans les DTO : l'e-mail d'un membre n'est expose qu'au MJ de la partie, jamais entre joueurs ; le DTO de recherche n'en expose aucun (AD-2). Motif retenu du principe utilisateur 'securiser plutot qu'empecher' : le risque concret est le lien d'invitation partageable (InviteLink accepte un nombre d'usages illimite), donc un membre n'est pas forcement quelqu'un que le MJ a choisi. L'invitation par e-mail reste inchangee.
- (event) Corrections du reviewer gate appliquees : AD-14 recablee sur user: seul (+ emit user:{id} par membre), contrat de forme PartySignalsDto (AD-3), AD-6 ecart assume + revokeSessions extraite, AD-10 corrigee (close vs delete, retrait par option), AD-7 renforcee (masque en parametre, propriete lockable distincte de fields, omission + lockedKeys, derived filtre solidairement), AD-1 (theme nullable, register(), hideFinishedParties front), AD-13 (adoption unique du theme local, liste des themes dans shared), AD-8 (status derive serveur nomme), AD-2 (e-mail membre reserve au MJ), nouvelle AD-15 (projection explicite de PartieDto). Lint final : 7 faux positifs 'partie:{id}'/'user:{id}' uniquement.
- (event) spine finalized
- (direction) UPDATE du 2026-08-05. Signal de changement : PRD revise (FR-45 a FR-48, derogations D-11 a D-13) et SPEC re-derive (CAP-18 a CAP-21), tous deux issus du run d'UX. Identifiants AD-1 a AD-15 preserves ; les nouvelles decisions prennent AD-16 et suivantes.
- (decision) AD-16 — PREFERENCES DE COUCHES DU CALENDRIER : modele relationnel, une ligne par couche active (UserCalendarLayer). Binds CAP-19, FR-46. Prevents : une troisieme forme de preference inventee pour un cas qu'AD-1 ne tranchait pas explicitement (l'ensemble de valeurs), et une migration de schema a chaque couche ajoutee. AD-1 prevoyait deja le multi-valué en relationnel : aucun amendement necessaire. Ecartes : six colonnes booleennes (migration par couche future) et une colonne tableau typee (ouvrait une troisieme forme, qu'AD-1 existe justement pour empecher). Choix utilisateur.
- (decision) AD-17 — UPLOAD D'IMAGE : le mecanisme de portrait (validation MIME, plafond 5 Mo, nettoyage EXIF, stockage disque) est EXTRAIT en utilitaire partage, utilise par le portrait de personnage ET par l'image de couverture de partie. Binds CAP-20, D-11. Prevents : deux chemins d'upload paralleles dont un seul beneficierait d'un durcissement futur — la validation MIME et le nettoyage EXIF sont des mecanismes de securite, les dupliquer garantit leur divergence. Cout assume : un refactor du code de portrait existant. Choix utilisateur.
- (decision) AD-18 — CALENDRIER PERSONNEL : un endpoint unique renvoie, pour une plage de dates, tout ce que les couches savent afficher (seances datees cross-partie, inscriptions ouvertes, votes). Binds CAP-19, D-6, D-13. Prevents : autant d'appels que de couches actives au chargement, et un nombre d'appels qui grandit a chaque couche ajoutee — meme divergence que celle qu'AD-3 previent pour les signaux de parties. Choix utilisateur.
- (constraint) AD-1 ETENDU (Rule amendee en place, identifiant conserve) : les colonnes typees sur User accueillent deux preferences scalaires supplementaires — mode d'affichage et tri par defaut des listes (CAP-18). La forme ne change pas.
- (decision) AD-19 — BANNIERE GENERATIVE : jamais persistee. Elle est derivee a l'affichage d'une graine calculee depuis l'identifiant de la partie ; ni la graine, ni les parametres tires, ni le rendu ne sont stockes. Binds CAP-20. Prevents : une story qui persisterait les parametres de tirage 'pour la stabilite' alors que le determinisme de la derivation la garantit deja, creant une seconde source de verite qui divergerait au moindre changement de regle de generation. Coherent avec la convention 'derive a la lecture, jamais persiste en double' deja etablie aux Paliers 5 et 8.
- (decision) AD-20 — ETATS DEPENDANTS DU LECTEUR : resolus par le CLIENT a partir des charges utiles existantes, aucun endpoint dedie. Binds CAP-5, CAP-12, D-12. Prevents : la construction d'un service serveur de calcul d'etat par lecteur, alors que PollOptionDto.votes porte deja les userId de tous les votants et que les scenarios brouillons vivent derriere un endpoint distinct — le compteur de chronologie differe donc deja de lui-meme. Redeviendrait justifie si l'on voulait masquer l'identite des autres votants ; ce jour-la, D-12 change d'ampleur.
- (event) REVIEWER GATE passe sur la mise a jour : lint deterministe propre (7 faux positifs 'partie:{id}'/'user:{id}'), puis 3 lentilles. Rapports dans reviews/review-rubric-update.md, review-verification-update.md, review-adversarial-update.md.
- (override) AD-20 REPOSAIT SUR UNE AFFIRMATION FAUSSE, etablie par verification code : scenarios.service.ts findAllForPartie ne filtre AUCUN statut (where: { partieId } seul, avec commentaire explicite 'aucun filtrage par statut — l'anti-spoil est un rendu frontend, jamais serveur'). GET /parties/:id/scenarios renvoie donc les BROUILLON a tout membre ; listDrafts est une vue MJ dediee, pas un endpoint qui les retire de la liste normale. Ma justification etait inversee. Danger concret : une story lisant l'AD telle qu'ecrite aurait pu supprimer le filtrage frontend comme redondant.
- (override) AD-18 CONTREDISAIT AD-9 sur le calendrier personnel : AD-9 y affiche les seances de l'utilisateur explicitement et legendees, AD-18 imposait 'indisponibilite sans identite de partie'. Le vocabulaire 'autre partie' n'a pas de sens dans un calendrier personnel — toutes les seances y sont celles de l'utilisateur. La clause de non-fuite ne vaut que dans le calendrier D'UNE PARTIE.
- (override) AD-18 CREAIT LE MODULE QUE LE 'PREVENTS' D'AD-4 INTERDIT : un CalendarModule agregeant des lectures scopees utilisateur est exactement le module fourre-tout qu'AD-4 decrit pour l'interdire. L'endpoint rejoint AvailabilityModule, proprietaire naturel du calendrier.
- (override) AD-17 SOUS-ESTIMAIT L'EXTRACTION, etabli par verification code : le plafond 5 Mo vit dans des DECORATEURS DE CONTROLEUR (FileInterceptor.limits + MaxFileSizeValidator + MulterExceptionFilter) donc non extractible ; le stockage disque est dans character.service.ts, pas dans portrait-storage.util.ts qui ne fait que lire, et il est couple a un verrou optimiste et a une emission SSE. Consommateurs a mettre a jour : ryuutama-pdf.service.ts (importe readPortraitFile) et character.service.spec.ts (jest.mock sur le chemin de image-mime.util). Et il existe deja un TROISIEME chemin d'upload (scenarios/document-storage.util.ts) que ma premisse 'deux chemins' ignorait.
- (override) AD-20 x AD-3 : collision de propriete sur l'etat 'vote en cours'. AD-3 en fait un PartySignalCode calcule serveur pour la liste ; AD-20 le disait resolu client. Pire, AD-20 etait structurellement inapplicable en liste puisqu'AD-3 interdit l'appel par partie — la liste n'a jamais PollOptionDto.votes.
- (assumption) AD-1 amendee : une seule paire listViewMode/listSort pour DEUX listes aux vocabulaires de tri disjoints (parties triees par urgence, personnages par niveau ou par partie). Modele de donnees faux, pas seulement sous-specifie.
- (question) DIMENSION SILENCIEUSE relevee par la rubrique : l'ecriture en masse des disponibilites (CAP-14, glissement sur plusieurs jours et creneaux) — N appels ou une ecriture groupee ? Non arbitre, et l'option groupee serait une derogation serveur non recensee. A trancher avec l'utilisateur au titre de P-5.
- (decision) CORRECTIFS DU GATE APPLIQUES (11 points). AD-16 : layerKey devient une union fermee dans shared, et User.calendarLayersSetAt distingue 'jamais configure' de 'tout eteint' (sans quoi un compte neuf ouvrait un calendrier vide). AD-17 : distingue ce qui est extractible (validation MIME, EXIF, gardes de chemin a parametrer, ecriture disque aujourd'hui dans character.service.ts) de ce qui ne l'est pas (plafond 5 Mo, porte par des decorateurs de controleur, a rejouer) ; nomme les 2 consommateurs a mettre a jour et le 3e chemin d'upload existant ; fixe l'autorisation MJ et le service par endpoint sous garde. AD-18 : heberge dans AvailabilityModule et non dans un CalendarModule neuf (qu'interdisait le Prevents d'AD-4), forme de charge utile fixee (objet indexe par couche), clause de non-fuite scopee au calendrier D'UNE PARTIE. AD-19 : point de derivation unique, graine derivee du SEUL identifiant de partie (ni nom ni cle de theme, sinon le renommage casserait le determinisme), priorite image/banniere fixee. AD-20 : justification corrigee et portee restreinte aux ecrans detenant la charge utile — la liste reste servie par AD-3.
- (decision) AD-1 CORRIGEE : quatre scalaires au lieu de deux — une paire de preferences PAR LISTE (parties et personnages trient sur des vocabulaires disjoints, une paire unique rendait la moitie des valeurs invalides). Les quatre champs sont valides contre des unions fermees declarees dans shared.
- (decision) AD-13 ETENDUE : le renommage medieval-steampunk -> atelier-cuivre emporte une migration des valeurs persistees de User.theme, indissociable de la story qui decoupe les fichiers de theme. Sans elle, tout compte ayant choisi ce theme le perd silencieusement.
- (decision) AD-15 ETENDUE : Partie.coverImageUrl fait partie de la projection PartieDto — une carte de partie ne declenche jamais de lecture dediee pour savoir si une image existe, ce qui ramenerait l'appel par partie qu'AD-3 interdit.
- (event) Lint deterministe apres correctifs : 7 remontees, toutes faux positifs (noms de canaux SSE). Spine a 499 lignes, 20 AD.
- (decision) AD-21 — DECLARATION DE DISPONIBILITE EN MASSE : un seul appel portant les N creneaux selectionnes, ecriture transactionnelle tout-ou-rien. Binds CAP-14, FR-32, D-14. Prevents : un geste unique produisant N appels (21 pour une semaine entiere, sous un limiteur de debit toujours actif — motif de fan-out deja responsable de deux bugs de production) et un etat partiel indecidable ou la moitie des creneaux est enregistree. Meme discipline que AvailabilityService.create() qui enveloppe deja ses ecritures dans $transaction ; findConflictsForCreate s'applique a chaque creneau du lot avant toute ecriture. Choix utilisateur, acte comme 14e derogation.
- (event) spine finalized (update du 2026-08-05) : 21 AD, 14 derogations, gate passe a 3 lentilles avec 11 correctifs appliques dont 3 collisions frontales et une affirmation factuellement fausse.
