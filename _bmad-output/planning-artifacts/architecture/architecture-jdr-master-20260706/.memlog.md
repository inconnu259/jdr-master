---
scope: Palier 4 — Infra e-mail & notifications
purpose: Input pour bmad-create-epics-and-stories
altitude: feature
updated: 2026-07-07T00:55
---

- (event) Spine initiée, coaching path, brownfield. Reprend l'item explicitement différé dans la spine Calendrier (2026-06-27) : "Notifications email | Palier 6 (SMTP)". Nouvelle spine dédiée plutôt qu'update, pour garder le titre/scope de la spine Calendrier exact.
- (constraint) Héritées (Palier 1/2, read-only) : PrismaService global jamais réimporté ; mutations exclusivement via couche Service, jamais dans un controller ; Angular standalone components + Signals, import type pour @master-jdr/shared, control-flow @if/@for ; auth session-based (AuthenticatedGuard) sur toutes les routes protégées.
- (decision) EmailModule generique unique (apps/api/src/email/), expose EmailService.sendMail(template, to, data). Pas de service dedie par cas d'usage. Appele par InvitationsService (FR-3), un nouveau NotificationsModule (FR-4), AuthService (FR-5/6).
- (decision) Reset password: nouveau modele Prisma PasswordResetToken (userId, token, expiresAt, usedAt), a l'image d'InviteLink. Pas de JWT sans stockage DB -- coherent avec le pattern existant, revocable, auditable.
- (decision) NotificationsModule (nouveau, apps/api/src/notifications/) possede le Cron de rappel de seance. Importe PartiesModule (lecture membres+nextSessionDate) et EmailModule. Choix motive: ni PartiesModule ni AuthModule ne doit connaitre la logique de planification, qui est un concept transverse distinct.
- (decision) Dedup rappel: champ Partie.reminderSentAt (DateTime?, nullable). Cron horaire verifie nextSessionDate - 24h <= now < nextSessionDate ET reminderSentAt IS NULL, envoie puis set reminderSentAt=now. Tout changement de nextSessionDate (poll.service.ts choseDate) reset reminderSentAt=null pour eviter un rappel perime (FR-4).
- (decision) Librairies: @nestjs-modules/mailer (v2.3.7, Nodemailer) + @nestjs/schedule (@Cron). Moteur de gabarit: handlebars (.hbs), 3 templates (invitation, reminder, password-reset), un seul layout neutre partage (cf Non-Goals PRD: pas de variation par theme).
- (decision) Config transport: MAIL_HOST/MAIL_PORT/MAIL_USER/MAIL_PASSWORD/MAIL_FROM via ConfigModule, lues une seule fois par EmailModule.forRootAsync. docker-compose.yml dev: service mailhog (image mailhog/mailhog, port SMTP 1025, UI HTTP 8025), MAIL_HOST=mailhog en dev. Prod: memes clefs pointant vers Brevo -- aucun changement de code entre les deux, uniquement .env.
- (decision) Frontend: routes publiques /forgot-password (formulaire email) et /reset-password/:token (nouveau mot de passe), dans apps/web/src/app/features/auth/. Formulaire d'invitation de partie existant etend son DTO pour accepter un email brut en plus du choix Invitation/InviteLink actuel -- pas de nouvelle page, un champ ajoute au composant existant.
- (decision) InviteLink gagne un champ targetEmail (String?, nullable) pour distinguer un lien genere par invitation-par-email (usage unique, cible une adresse precise) d'un lien ouvert partageable existant (targetEmail=null). InvitationsService.inviteByEmail(partieId, inviterId, email): user existant -> reutilise invite() existant + envoie template invitation; sinon cherche InviteLink{partieId, targetEmail: email, revoked:false, expiresAt>now} et le renvoie si trouve, sinon en cree un nouveau maxUses:1. Resout FR-3 (pas de doublon sur re-invite).
- (event) elicitation terminee, passage a la redaction de la spine
- (event) spine redigee (7 AD), status draft -> revue
- (decision) Amendement post-finalize (recherche web pendant create-story 5.1) : MailHog remplace par Mailpit (axllent/mailpit) -- MailHog non maintenu depuis 2020. Mailpit est drop-in compatible (memes ports 1025/8025, meme API HTTP). Egalement corrige: pas de ConfigModule dans ce projet, EmailModule lit process.env directement (convention existante main.ts/prisma.service.ts).
